trigger:
- master

jobs:
  - job: basicBuild
    pool: 
     name: MS Default
     vmImage: 'ubuntu-latest'
  


    steps: 
    - checkout: self
      submodules: recursive

    - task: Bash@3
      inputs:
        targetType: 'inline'
        script: |
          # Write your commands here
          find . |grep -v git

  - deployment: 
    environment: DevEnv

  - deployment: 
    environment: QAEnv





